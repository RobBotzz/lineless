info:
  name: get location (no location set)
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventIdNoLocation}}/location
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
