info:
  name: get location
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}/location
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
