info:
  name: get location (no auth)
  type: http
  seq: 4

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}/location
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
