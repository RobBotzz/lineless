info:
  name: get product
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/products/{{productId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
