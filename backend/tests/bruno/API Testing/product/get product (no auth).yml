info:
  name: get product (no auth)
  type: http
  seq: 9

http:
  method: GET
  url: http://{{host}}:{{port}}/api/products/{{productId}}
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
