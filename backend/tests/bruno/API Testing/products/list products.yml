info:
  name: list products
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/stands/{{standId}}/products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
