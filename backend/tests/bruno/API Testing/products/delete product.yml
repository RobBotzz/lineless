info:
  name: delete product
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{host}}:{{port}}/api/products/{{productId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
