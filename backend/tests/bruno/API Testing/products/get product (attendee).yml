info:
  name: get product (attendee)
  type: http
  seq: 8

http:
  method: GET
  url: http://{{host}}:{{port}}/api/products/{{productId}}
  auth:
    type: none
  headers:
    - name: Cookie
      value: sessionId=test-session
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
