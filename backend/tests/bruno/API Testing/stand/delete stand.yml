info:
  name: delete stand
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{host}}:{{port}}/events/{{eventId}}/stands/{{standId}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
      enabled: true
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
