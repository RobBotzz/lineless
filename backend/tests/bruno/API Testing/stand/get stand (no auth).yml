info:
  name: get stand (no auth)
  type: http
  seq: 9

http:
  method: GET
  url: http://{{host}}:{{port}}/api/stands/{{standId}}
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
