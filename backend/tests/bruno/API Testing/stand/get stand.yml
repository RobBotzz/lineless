info:
  name: get stand
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}/stands/{{standId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
