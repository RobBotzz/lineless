info:
  name: list stands
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}/stands
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
