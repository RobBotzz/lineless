info:
  name: organizer auth invalid
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events
  auth:
    type: none
  headers:
    - name: Authorization
      value: Bearer invalid-organizer-token
      enabled: true

tests: |
  test("invalid organizer token is rejected", function () {
    expect(res.status).to.equal(401);
  });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
