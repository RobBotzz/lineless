info:
  name: attendee auth missing
  type: http
  seq: 4

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}
  auth:
    type: none

tests: |
  test("missing attendee session is rejected", function () {
    expect(res.status).to.equal(401);
  });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
