info:
  name: attendee auth valid
  type: http
  seq: 6

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}
  auth:
    type: none
  headers:
    - name: X-Attendee-Session-ID
      value: "{{attendeeSessionId}}"
      enabled: true

tests: |
  test("valid attendee session is accepted", function () {
    expect(res.status).to.equal(200);
  });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
