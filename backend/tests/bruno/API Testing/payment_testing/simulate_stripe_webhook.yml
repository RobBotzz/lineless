info:
  name: 5. Simulate Stripe Webhook (Overage)
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/webhooks/stripe
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": "evt_test_simulated_001",
        "type": "payment_intent.amount_capturable_updated",
        "data": {
          "object": {
            "id": "pi_3TcpaCIzZPlH0JKO1GjWEFCI",
            "amount_capturable": 2000,
            "metadata": {
              "tabId": "12528cce-17ad-42d8-8775-f43ae25c72c4"
            }
          }
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
