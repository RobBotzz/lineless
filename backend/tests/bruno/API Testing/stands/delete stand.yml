info:
  name: delete stand
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{host}}:{{port}}/api/stands/{{standId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
