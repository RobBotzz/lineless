info:
  name: delete event
  type: http
  seq: 1

http:
  method: DELETE
  url: http://{{host}}:{{port}}/api/events/{{eventId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
