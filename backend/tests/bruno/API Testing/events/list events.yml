info:
  name: list events
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
