info:
  name: get event
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/events/{{eventId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
