meta {
  name: 4 cash payments
  seq: 4
}

docs {
  Cash-payment coverage for the organizer/cashier-only flow. The folder logs in
  the organizer, discovers the event's CASHIER stand, logs in that cashier,
  creates a cash order, confirms it, refunds it, and verifies attendee/tab-order
  bypasses are rejected.
}
