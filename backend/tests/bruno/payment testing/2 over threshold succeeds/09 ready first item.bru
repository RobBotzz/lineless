meta {
  name: 09 ready first item
  type: http
  seq: 9
}

post {
  url: http://{{host}}:{{port}}/api/orders/{{firstOrderId}}/items/{{firstItemId}}/ready
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{operatorToken}}
}

tests {
  test("first item is ready", function () {
    expect(res.status).to.equal(200);
    const item = res.body.items.find(i => i._id === bru.getVar("firstItemId"));
    expect(item.readyAt).to.be.a("string").and.not.empty;
  });
}

settings {
  encodeUrl: true
}
